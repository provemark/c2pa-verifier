# c2patool's JSON for the certificate-validity variants (SPEC-044)

`c2patool ../../validity/<name>.png --settings ../../validity/throw-away-root.settings.json`,
c2patool 0.28.0 and 0.27.22, recorded 2026-09-25 by
`bin/make-spec044-variants.php`, as `<name>--<version>.json`. The table is
in `../../validity/README.md`.
