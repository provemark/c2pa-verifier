# c2patool's JSON on the absence variants (step 48)

`c2patool ../../absence/<name>.png` and the same with
`--settings ../../absence/throw-away-root.settings.json` (`-trusted`),
c2patool 0.27.22, recorded 2026-09-22, unchanged. The root is the
throw-away hierarchy's of the run that made the variants; regenerating
the variants regenerates these too. See `../../absence/README.md` for
what each says.
