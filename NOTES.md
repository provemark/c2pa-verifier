# Notes

The record of what was built, step by step, written for someone outside the
project. One file per step under `notes/`; this page is the index. Each note
keeps *measured* (a command was run, and which) apart from *reasoned* (a
conclusion from reading). The plan these steps follow is
[`docs/milestones.md`](docs/milestones.md); who did what, per session, is
[`AI-LOG.md`](AI-LOG.md).

| Step | Date | What | Note |
|---|---|---|---|
| 01 | 2026-09-19 | M0: the skeleton — package, tool chain, SPEC-000, CI, documents | [`notes/step-01-m0-skeleton.md`](notes/step-01-m0-skeleton.md) |
| 02 | 2026-09-19 | The signed JPEG fixture; what a JPEG is; APP11 pieces measured; how c2patool treats gaps and swaps | [`notes/step-02-jpeg-fixture.md`](notes/step-02-jpeg-fixture.md) |
| 03 | 2026-09-19 | The first code: the JPEG extractor; a wrong fixture found and fixed; c2patool ignores LBox in continuation pieces, AC7 kept stricter; amendment 1: two fixtures for the marker table and the end-of-file probe, a wrong claim caught by a mutation test | [`notes/step-03-jpeg-extractor.md`](notes/step-03-jpeg-extractor.md) |
| 04 | 2026-09-19 | The signed PNG fixture; what a PNG is; the `caBX` chunk measured; ten variants through c2patool; the CRC and LBox are not checked by c2pa-rs | [`notes/step-04-png-fixture.md`](notes/step-04-png-fixture.md) |
| 05 | 2026-09-20 | The PNG extractor; AC14 found the end-of-file probe naming the wrong chunk; CRC, LBox and empty chunk stricter than c2patool | [`notes/step-05-png-extractor.md`](notes/step-05-png-extractor.md) |
| 06 | 2026-09-20 | The signed WebP fixture; what a RIFF file is; the `C2PA` chunk and its pad byte measured; fifteen variants through c2patool; c2pa-rs takes the first `C2PA`, ignores the form type, honours the RIFF size | [`notes/step-06-webp-fixture.md`](notes/step-06-webp-fixture.md) |
| 07 | 2026-09-20 | The WebP extractor; the RIFF size checked against the file first; seven variants stricter than c2patool; **M1 complete** | [`notes/step-07-webp-extractor.md`](notes/step-07-webp-extractor.md) |
| 08 | 2026-09-20 | One `StreamReader` for the three extractors; the JPEG probe replaced by the file-end look-up (SPEC-001 AC16); 60 lines fewer | [`notes/step-08-stream-reader.md`](notes/step-08-stream-reader.md) |
| 09 | 2026-09-20 | The manifest store from the inside: the JUMBF tree, the CBOR inventory (the subset is small — measured), the detached COSE payload, `claim_version` from the label; a 2022 Adobe file (claim v1, JSON box, timestamp) through our extractor | [`notes/step-09-manifest-store-inside.md`](notes/step-09-manifest-store-inside.md) |
| 10 | 2026-09-21 | Twenty-three malformed manifest stores through c2patool before SPEC-005's approval; three `Valid`s where SPEC-005 errs (root label, root LBox, toggle bit); `unknown-uuid` is the claim's error, not the box's | [`notes/step-10-jumbf-variants.md`](notes/step-10-jumbf-variants.md) |
| 11 | 2026-09-21 | The JUMBF parser: the store as a tree of boxes, `payload()` hashing to the claim's values, unknown boxes kept, compressed manifests refused; the `Support` layer for `Bytes::hex()` | [`notes/step-11-jumbf-parser.md`](notes/step-11-jumbf-parser.md) |
| 12 | 2026-09-21 | Sixteen CBOR values recorded as data; four faults in a signed claim through c2patool — indefinite lengths, duplicate keys and non-shortest ints are all *read* by c2pa-rs | [`notes/step-12-cbor-vectors.md`](notes/step-12-cbor-vectors.md) |
| 13 | 2026-09-21 | The CBOR decoder: the measured subset, every RFC 8949 Appendix A/F vector the spec lists, sixteen recorded values reproduced; three test-file mistakes caught on the first run | [`notes/step-13-cbor-decoder.md`](notes/step-13-cbor-decoder.md) |
| 14 | 2026-09-21 | c2patool's JSON for the four fixtures recorded; fifteen wrong claims through c2patool — schema faults are errors, a mistyped hash is read, broken JSON is a status code | [`notes/step-14-claim-variants.md`](notes/step-14-claim-variants.md) |
| 15 | 2026-09-21 | The Manifest layer: claim v1 and v2 typed, assertions decoded by content type, references resolved, the JSON view the sister library reads — AC6 green, **M2 complete** | [`notes/step-15-manifest-layer.md`](notes/step-15-manifest-layer.md) |
| 16 | 2026-09-21 | The four signatures verified by hand with `ext-openssl` (ES256 via DER, PS256 via an `rsassaPss` key), one flipped byte → invalid, three broken variants through c2patool; `cose-lib` measured — it cannot read the Adobe certificate; ADR-0001's "cose-lib first" proposed for amendment | [`notes/step-16-cose-signature.md`](notes/step-16-cose-signature.md) |
| 17 | 2026-09-21 | Eleven broken COSE structures through c2patool: a present (empty) payload is `Valid` for c2pa-rs and an error here; five structural faults wear a timestamp label there | [`notes/step-17-cose-variants.md`](notes/step-17-cose-variants.md) |
| 18 | 2026-09-21 | `CoseSign1`: tag 18, four items, headers and chain read fail-closed, the `Sig_structure` built by the project's one CBOR encoder — the step-16 vectors reproduced byte for byte | [`notes/step-18-cose-sign1.md`](notes/step-18-cose-sign1.md) |
| 19 | 2026-09-21 | Fourteen signature vectors from throw-away keys (deleted), each verified twice; the P-521 DER long-form length bug found before any code; OpenSSL refuses a PSS-parameter mismatch with −1 | [`notes/step-19-signature-vectors.md`](notes/step-19-signature-vectors.md) |
| 20 | 2026-09-21 | `SignatureVerifier`: ES256/384/512, PS256/384/512 (two paths by key type), Ed25519 — key checked against the algorithm before the arithmetic; the four fixtures verify, fourteen vectors behave as recorded; the first cryptography in `src/` | [`notes/step-20-signature-verifier.md`](notes/step-20-signature-verifier.md) |
| 21 | 2026-09-21 | c2patool's JSON for three broken files; its url for `assertion.json.invalid` is a bare label where every other status has a JUMBF URI — AC7 corrected by the measurement | [`notes/step-21-report-oracle.md`](notes/step-21-report-oracle.md) |
| 22 | 2026-09-21 | The Report layer: C2PA 2.4 §15 codes verbatim, status codes on the Manifest and Cose exceptions, the claim-signature check as statuses in c2patool's shape — M3's "done when" measured end to end; **M3 complete** | [`notes/step-22-report-layer.md`](notes/step-22-report-layer.md) |
| 23 | 2026-09-21 | The hash binding measured before M4: hashed URIs and the streaming data hash match for all four fixtures; the exclusion is the store plus the format's framing (JPEG 32, PNG 12, WebP 8 bytes; the WebP pad byte is hashed); thirteen variants through c2patool, two with no report at all, one corrected by its own measurement | [`notes/step-23-binding-measured.md`](notes/step-23-binding-measured.md) |
| 24 | 2026-09-21 | Eight hashed-URI variants for SPEC-011 through c2patool: every entry reported after a mismatch, the entry-level `alg` honoured, a truncated hash a report; undeclared, duplicate and missing-alg a hard error there; c2patool's redaction check is one rule (no actions) and does not notice a "redacted" box that is still present | [`notes/step-24-hashed-uri-variants.md`](notes/step-24-hashed-uri-variants.md) |
| 25 | 2026-09-21 | `HashedUriCheck`, the first file of the `Hash` layer: every entry hashed against its box, undeclared boxes by identity, unknown boxes included, redactions refused until M7 — ten tests red → green, 163 in all; three divergences from c2patool kept, all failures on both sides | [`notes/step-25-hashed-uri-check.md`](notes/step-25-hashed-uri-check.md) |
| 26 | 2026-09-21 | The store's file range equals the writer's exclusion byte for byte on all four fixtures (framing 32/12/12/8); twelve data-hash variants through c2patool — three with a recomputed hash confirmed `dataHash.match`, the multiple-binding url is the manifest's (SPEC-012 amendment 1); three script bugs caught by a parse probe before the oracle | [`notes/step-26-data-hash-variants.md`](notes/step-26-data-hash-variants.md) |
| 27 | 2026-09-21 | `DataHashCheck`: the asset hashed in 64 KiB chunks with the exclusions skipped, the store's exclusion required to equal the store's file range, exactly one hard binding, the first informational code; ten tests red → green, 173 in all; a 48 MiB file under 4 MiB of memory — **M4 complete** | [`notes/step-27-data-hash-check.md`](notes/step-27-data-hash-check.md) |
| 28 | 2026-09-21 | The ten SPEC-013 tests, seen red; AC10 walks every recorded c2patool JSON through the front door — two files the spec called subset-only were equal (a `dataHash.match` is not a failure), corrected before any code | (this step's record is in `AI-LOG.md` and `docs/milestones.md`; the note is step 29's) |
| 29 | 2026-09-21 | `Verifier::verify()`: format → store → manifest → signature → hashed URIs → data hash (skipped when the claim did not vouch for it); `VerificationReport` in c2patool's shape; the sister parser reads it; the drift alarm over 22 files — state equal on all, failure sets equal on 16 and a subset on the 6 decided; where the two c2pa-rs divergences live, with line numbers — ten tests red → green, 183 in all | [`notes/step-29-verifier.md`](notes/step-29-verifier.md) |
| 30 | 2026-09-21 | Trust measured before M5: the two test hierarchies (EC and RSA-PSS, same names), the fixtures' x5chains (no root in the EC ones), c2patool under nine settings (`Trusted` via anchors or allowed list; `trust_config` only adds EKUs — read in c2pa-rs), what §14.5's profile check is line by line, and what `ext-openssl` can do (`openssl_x509_verify` per link; `checkpurpose` needs files and now) | [`notes/step-30-trust-measured.md`](notes/step-30-trust-measured.md) |
| 31 | 2026-09-21 | Three trust variants for SPEC-014: the intermediate cut from the protected `x5chain` and the `pad` grown by the same 625 bytes (nothing else moves; only the signature breaks); an intermediate as the only anchor is `Trusted` (`PARTIAL_CHAIN`), the allowed list wins over a wrong root, trust does not rescue `pixel-changed` — no criterion contradicted | [`notes/step-31-trust-variants.md`](notes/step-31-trust-variants.md) |
| 32 | 2026-09-21 | `ChainCheck`, the first `Trusted`: settings whole or absent, the allowed list first, the walk on `openssl_x509_verify` to a certificate equal to or signed by an anchor — a name is not a proof; `Trusted`/`Valid`/`Invalid` by the measured rule; `validation_status` omitted when empty; OpenSSL's `checkpurpose` agrees on every fixture — ten tests red → green, 193 in all | [`notes/step-32-chain-check.md`](notes/step-32-chain-check.md) |
| 33 | 2026-09-21 | The certificate profile measured: a throw-away hierarchy (keys deleted by the script), the PNG re-signed twelve times with one departure each, c2patool with the root as anchor — nine `invalid`/`expired`, `good` and `eku-c2pa` `Trusted`, and `nonRepudiation`-only **`Trusted`** (c2pa-rs's KU rule read to the end: digitalSignature *or* nonRepudiation *or* keyCertSign; AKI required) | [`notes/step-33-profile-measured.md`](notes/step-33-profile-measured.md) |
| 35 | 2026-09-21 | `CertificateProfileCheck` (eight rules, one `.invalid` per fault, `.expired` at now until M6) and `signature_info` (serial hex → decimal, no gmp); the profile always, trust without settings → `untrusted` as c2patool (found by M5's own "done when"); an RSA-PSS key recognised by its SPKI OID — ten tests red → green, 203 in all; **M5 complete** | [`notes/step-35-certificate-profile.md`](notes/step-35-certificate-profile.md) |
| 36 | 2026-09-21 | The official test files: 26 JPEGs from `c2pa-org/public-testfiles` through the front door next to c2patool — 20 states equal, thirteen `Trusted` and five `Invalid` code for code on files from another writer; two findings: CBOR floats in four camera files (SPEC-006 too strict) and one `Trusted` on a file tampered only in an ingredient manifest (fail closed until M7, Maurice's decision) | [`notes/step-36-public-testfiles.md`](notes/step-36-public-testfiles.md) |
| 37 | 2026-09-21 | Floats decode (SPEC-006 amendment 2: half by hand, single/double by `unpack`); the four camera files parse — Nikon then agrees code for code, Truepic shows three things: c2pa-rs also allows SHA-384/512 with RSA (SPEC-015 amendment 3, fixed), one-day certificates that need the timestamp (M6), and an exclusion that covers more than the store (SPEC-012's exact-range rule; a decision) | [`notes/step-37-floats.md`](notes/step-37-floats.md) |
| 38 | 2026-09-21 | Two rules from the official files: the data hash's exclusion must *cover* the store, not equal it (Truepic excludes the file head; SPEC-012 amendment 5), and a store with more than one manifest is `Invalid` until M7 (SPEC-013 amendment 5, AC11 — the file tampered only in an ingredient was `Trusted` here); the 24 official files as a second drift alarm with eleven named, stricter-on-purpose exceptions — 204 tests | [`notes/step-38-cover-and-multi-manifest.md`](notes/step-38-cover-and-multi-manifest.md) |
| 39 | 2026-09-21 | The 33 c2pa-rs fixtures as a third corpus and drift alarm; three findings: indefinite-length CBOR in nine claims (SPEC-006 amendment 3, accepted and bounded), a `null` `claim_generator_info` (SPEC-007 amendment 4), and a CAWG identity assertion the oracle validates and we did not — refused until a spec does (SPEC-013 amendment 7); 8 of 17 equal, 9 stricter by name, none more lenient — 206 tests | [`notes/step-39-c2pa-rs-corpus.md`](notes/step-39-c2pa-rs-corpus.md) |
| 40 | 2026-09-22 | The timestamp measured before M6: five TSAs (DigiCert 2022/2023/2025, Truepic) in bytes, the countersigned bytes proven by hand on four tokens and one tampered file, c2patool's `timeStamp.*` over 41 JSONs (informational — never `Invalid`; the timestamp's one effect is the *time* validity is judged at), c2pa-rs's checks in order, `ext-openssl`'s reach (a DER reader for `TSTInfo` is unavoidable; the CMS signature verifies with `openssl_verify`, no temp file), `CA_ct.jpg`'s minute 63, and c2patool's `timeStamp.trusted` without anchors as an unresolved point — material for ADR-0004 | [`notes/step-40-timestamp-measured.md`](notes/step-40-timestamp-measured.md) |
| 41a | 2026-09-22 | The SPEC-016 tests seen red (64, naming the three missing classes); a test-side DER walker for the eleven AC7 patches, each proven with `openssl asn1parse` before the tests ran — two helper faults and one runaway caught that way; 38 timestamped corpus files (one PNG), the bounds measured (d=18, 311 elements) and pinned; SPEC-016 amendment 1 | [`notes/step-41-timestamp-tests.md`](notes/step-41-timestamp-tests.md) |
| 41b | 2026-09-22 | `src/Asn1/` (the DER reader: ten tags, definite and minimal lengths, bounded, every fault with its offset) and `src/Timestamp/` (header, token, SignedData, SignerInfo, TSTInfo) — 64 tests red → green, 270 in all; the first green run corrected seven test literals and one assumption (the signer is the certificate the `sid` names, not the first: Truepic lists its root first; `ocsp*.jpg` carry an ECDSA TSA) — SPEC-016 amendment 2, `implemented` | [`notes/step-41-timestamp-tests.md`](notes/step-41-timestamp-tests.md) |
| 42a | 2026-09-22 | The SPEC-017 tests seen red (15; ten on the missing `TimestampCheck`, one on behaviour: `exp-test1` still expired under the cross-certificate anchor); two public TSA anchors cut out of the corpus tokens and c2patool measured under them — the Truepic files become `Trusted`, `exp-test1` loses its `expired`; the SPEC-016 helpers moved to `tests/Support/`; `_NO_TIMESTAMP` renamed `_TSA_NOT_CONFIGURED` with `exp-test1` added | [`notes/step-42-timestamp-check-tests.md`](notes/step-42-timestamp-check-tests.md) |
| 42b | 2026-09-22 | `TimestampCheck` and `TimestampResult`: c2pa-rs's seven steps, RSA/ECDSA/PSS, the TSA chain ordered from the token, trust through M5's new seams, six codes, `signature_info.time`, the trusted time to SPEC-015 — 15 red → green, 285 in all; six test literals and nine older tests adjusted (SPEC-010 #5, SPEC-013 #8, SPEC-014 #2, SPEC-015 #4, SPEC-017 #1); 35 corpus tokens equal to c2patool, the Truepic files `Trusted` with their root as anchor; **M6 complete** | [`notes/step-42-timestamp-check-tests.md`](notes/step-42-timestamp-check-tests.md) |
| 43 | 2026-09-22 | Fixtures from other writers: nine repositories surveyed (public-testfiles' 2.2 tree is empty; the conformance suite has no assets), five files taken — OpenAI, Amazon Bedrock (the first ES384 signer; validated unchanged), TrustNXT `c2pa-ts`, Photoshop with a remote manifest, a CAWG ICA file; three findings against c2patool: negative nonces are legal DER and two tokens carry them (`malformed` here), `genTime` fractions are kept by c2patool in `signature_info.time` (dropped here), a remote-manifest URL goes unreported; nothing more lenient anywhere | [`notes/step-43-more-fixtures.md`](notes/step-43-more-fixtures.md) |
| 44 | 2026-09-22 | Step 43's findings fixed with tests seen red on the writers files: signed nonces, fractions kept (`time` byte-equal to c2patool's on OpenAI and `c2pa-ts`), and — uncovered by the fix — the CMS signature must be verified over the DER-*sorted* SET of attributes (RFC 5652 §5.4, X.690 §11.6): every TSA before wrote them sorted, `c2pa-ts` does not; raw R‖S ECDSA accepted DER-safely; a remote manifest reported by URL, never fetched; the writers corpus as the fourth drift alarm — 293 tests | [`notes/step-44-writers-fixes.md`](notes/step-44-writers-fixes.md) |
| 45 | 2026-09-22 | Light fuzzing with a replayable script: 70 870 mutated files (bit flips, truncations, random blocks, flips and cuts inside the manifest store) through the verifier — no exception escaped any parser, every one of the 312 mutations that stayed `Valid` is `Valid` at c2patool too and lands in bytes the format leaves uncovered (the COSE pad, the timestamp token, segment headers) | [`notes/step-45-fuzz.md`](notes/step-45-fuzz.md) |
| 46 | 2026-09-22 | Writers from Wikimedia Commons: a Pixel 10 photo (public domain — a three-month camera certificate, Google's own TSA, an Ultra HDR second asset with its own hashes) and a Lightroom Classic export (CC BY-SA 4.0 — Adobe's production certificate); both equal to c2patool, the Pixel `Trusted` under Google's intermediates; Leica, Sony and Samsung uploads all lose their manifest on export; and a hole found by asking what a manifest without any hard binding does: the data-hash check is skipped and a signed one would be `Valid` — step 47 | [`notes/step-46-more-writers.md`](notes/step-46-more-writers.md) |
| 47 | 2026-09-22 | The wrong `Valid`, shown and closed: a properly signed manifest with no hard binding (a re-signed variant with throw-away keys) was `Valid` — `Trusted` with its root as anchor — because the data-hash check ran only when `hash.data`'s hashed URI matched and never when there was none; c2patool refuses the file outright. AC15 red on the verdict, the gate turned around, `claim.hardBindings.missing`; the lesson for the corpus policy: for every "X when Y matches", a signed variant where Y is absent | [`notes/step-47-no-hard-binding.md`](notes/step-47-no-hard-binding.md) |
| 48 | 2026-09-22 | The absence audit: an inventory of every gate and what an absence does there (all refused at parse or by a check, or allowed — except one), then four signed variants with the data hash re-bound where a box was removed; the one: a v2 manifest without an actions assertion is `Valid` here and `Invalid` at c2patool — more lenient than the oracle, a second wrong `Valid`; the thumbnail, the placement of `hash.data`, an empty created list: agreement or both refuse; SPEC-018 (the actions assertion, two normative rules) proposed | [`notes/step-48-absence-audit.md`](notes/step-48-absence-audit.md) |
| 49a | 2026-09-22 | The SPEC-018 tests seen red, the verdict first (`no-actions.png` is `Valid`); two more signed actions variants — a first action `c2pa.edited` (c2patool `Invalid`, the manifest url) and an empty list (c2patool exit 1, no report); the v1 rule through a seam, since no signed v1 fixture can be made from a v2 claim | [`notes/step-49-actions-check.md`](notes/step-49-actions-check.md) |
| 49b | 2026-09-22 | `ActionsCheck`: the three rules, `assertion.action.malformed`, the unread gate in the Verifier — 6 red → green, 301 tests; c2patool's url for this rule is the bare manifest label (copied, amendment 2); the second wrong `Valid` closed, the actions content family named as the remaining leniency until M7 | [`notes/step-49-actions-check.md`](notes/step-49-actions-check.md) |
| 50 | 2026-09-22 | Release hygiene: the README no longer says "M0: skeleton" — status, use, the three verdicts, the trust-settings shape; `SECURITY.md` naming the two wrong `Valid`s the project found in itself; `CONTRIBUTING.md`; `CHANGELOG.md`; `docs/comparison.md` with the less / equal / different tables by name | [`notes/step-50-release-hygiene.md`](notes/step-50-release-hygiene.md) |
| 51 | 2026-09-22 | The amendment list: 51 amendments over 17 specs on one page, by weight — sixteen that change a rule of the verifier (six of them decided by Maurice at the time, SPEC-013 #7 the one with substance still open), fifteen that change the report or API, twenty literals; the floats amendment of SPEC-006, a stub since step 37, written out | [`notes/step-51-amendments-for-confirmation.md`](notes/step-51-amendments-for-confirmation.md) |
| 52a | 2026-09-22 | The SPEC-019 tests seen red: the command's stdout must be the library's `toJson()` byte for byte, exit 0 `Trusted`/`Valid`, 1 `Invalid` (c2patool: 0), 2 no report (a missing settings file included — c2patool ignores it); `fopen` on a directory succeeds, so the command refuses directories itself | [`notes/step-52-command-line.md`](notes/step-52-command-line.md) |
| 52b | 2026-09-22 | The command line built: `Cli\Command::run()` and the shim; the corpus criterion found `toJson()` throwing on OpenAI's file (a byte string in `claim_generator_info` rendered raw — SPEC-007 amendment 5); 314 tests | [`notes/step-52-command-line.md`](notes/step-52-command-line.md) |
| 53 | 2026-09-22 | Ingredients measured before M7: the spec's two validation methods, c2pa-rs's walk and its report (deltas, attested failures dropped, the three-state rule), the 18 multi-manifest files — legacy claim-hash in 12, box-payload hash in 6, no redactions, one update manifest; three specs proposed | [`notes/step-53-ingredients-measured.md`](notes/step-53-ingredients-measured.md) |
| 54a | 2026-09-22 | The SPEC-020 tests seen red; eleven signed ingredient variants (an assertion box added, the binding re-bound, re-signed) measured against c2patool — six hard exits, and two malformed rules the specification has and c2pa-rs does not | [`notes/step-54-ingredient-assertion-and-graph.md`](notes/step-54-ingredient-assertion-and-graph.md) |
| 54b | 2026-09-22 | The graph built: the ingredient assertion as a value object with its malformed rules, the walk with bounds and cycles, `ingredientDeltas` and `ingredients` in the report as c2patool prints them; four measurements corrected the approved text (amendments 1–3); 327 tests, no verdict changed | [`notes/step-54-ingredient-assertion-and-graph.md`](notes/step-54-ingredient-assertion-and-graph.md) |
| 55 | 2026-09-22 | Before SPEC-021: what checking the ingredient manifests would say — the two hash forms counted, the dropping rule measured (14 of 17 files' delta failures and 16 of 18 states equal to c2patool), the one apparent leniency chased down to a settings artefact | [`notes/step-55-ingredient-validation-measured.md`](notes/step-55-ingredient-validation-measured.md) |
| 56a | 2026-09-22 | The SPEC-021 tests seen red; three signed variants — the one that matters carries a broken ingredient signature behind a *matching* box hash, and c2patool reports both — plus the measurement that c2patool's guard covers every box of the active manifest, not only its signature | [`notes/step-56-ingredient-validation.md`](notes/step-56-ingredient-validation.md) |
| 56b | 2026-09-22 | The ingredient manifests validated: box hash, then everything the active manifest gets except the data hash, with the writer's recorded faults dropped and the active-manifest guard kept; the multi-manifest refusal lifted — seventeen files measured instead of refused, sixteen verdicts exactly c2patool's; 336 tests | [`notes/step-56-ingredient-validation.md`](notes/step-56-ingredient-validation.md) |
| 57a | 2026-09-22 | The SPEC-022 tests seen red; six update-manifest variants measured (the JPEG's one APP11 segment rebuilt around each edited store, round trip asserted first); a leniency found — a standard manifest with two `parentOf` ingredients is `Trusted` here, `manifest.multipleParents` at c2patool | [`notes/step-57-update-manifests.md`](notes/step-57-update-manifests.md) |
| 57b | 2026-09-22 | Update manifests read and judged: §11.2.3's rules, the binding found up the `parentOf` chain and its stale exclusion adjusted to the store, `manifest.multipleParents` closing a leniency; four measured corrections along the way (one of them stricter than c2patool by the specification); 345 tests — **M7 complete** | [`notes/step-57-update-manifests.md`](notes/step-57-update-manifests.md) |
| 58 | 2026-09-22 | The seventeen amendments since step 51 on one page, by weight, with two of them flagged for a second look: the first case where this verifier is stricter than c2patool on a rule c2patool has but cannot reach, and the amendment that moves eighteen verdicts; **all three groups confirmed** | [`notes/step-58-amendments-for-confirmation.md`](notes/step-58-amendments-for-confirmation.md) |
| 59 | 2026-09-22 | What the fixtures actually proved, measured — and the holes filled: every signature algorithm in every container, and the sha384/sha512 data hashes, as files c2patool also validates; the fifth drift alarm — and it immediately found a verdict that depended on the PHP version (Ed25519 `Invalid` on 8.3) | [`notes/step-59-coverage-matrix.md`](notes/step-59-coverage-matrix.md) |
| 60 | 2026-09-22 | The absence audit for M7, on the first two-manifest store this project built itself: an ingredient without actions is `Invalid` here and there; a manifest nobody references is ignored by both even with a broken signature, and an ingredient assertion without `claimSignature` is read by both — named, not silent | [`notes/step-60-m7-absence-audit.md`](notes/step-60-m7-absence-audit.md) |
| 61 | 2026-09-22 | A second implementation as oracle (the Go verifier, 257 files): nothing it found that we missed; a false `Invalid` of *theirs* proven by hashing a file both ways; our thirteen strictnesses confirmed as deliberate, and two profile rules where we follow c2pa-rs and they do not | [`notes/step-61-second-oracle.md`](notes/step-61-second-oracle.md) |
| 62 | 2026-09-22 | The repository read as a stranger would read it, before it is one: no secrets, no local paths, no dead links, fixture provenance complete — but the README still refused multi-manifest stores a milestone after M7 closed, a `composer require` would have pulled 62.9 MB of fixtures (now 1.9 MB), and the repository had no description | [`notes/step-62-public-readiness.md`](notes/step-62-public-readiness.md) |
| 63 | 2026-09-22 | SPEC-023 tests first: thirteen red, each naming the function it wants — and the first red was one fatal that took all 353 other tests down with it, so the require is guarded. Two criteria were already true today, so each is asserted against a case it must catch; AC3's is the real 62.9 MB archive from before step 62 | [`notes/step-63-published-package.md`](notes/step-63-published-package.md) |
| 63b | 2026-09-22 | `bin/package-check.php`, 366 tests green — and three lessons: AC6 as approved could not pass (a library without an autoloader loads nothing, so the criterion now describes Composer's own layout, amendment 1); `git archive` without `--worktree-attributes` is *not* "without `.gitattributes`", so AC3 archives the real commit before the file existed, 62.9 MB; and two traps recorded — canonicalised `phar://` paths, and a Pest `skip()` closure that may not be `static` | [`notes/step-63-published-package.md`](notes/step-63-published-package.md) |
| 64 | 2026-09-22 | The three amendments since step 58, for confirmation: the Ed25519 key-kind read from the SPKI OID (a verdict that depended on the PHP version), the matrix as a fifth drift alarm, and SPEC-023's AC6 rewritten before implementation. 51 + 17 + 3 = 71, which is what the specs hold | [`notes/step-64-amendments-since-58.md`](notes/step-64-amendments-since-58.md) |
| 65 | 2026-09-22 | Mutation testing: would the tests notice if the code were wrong? **98.06%** — 4 547 killed, 90 untested. Three were worth acting on, and one overturned my own reading: deleting the EMSA-PSS trailer-byte check makes `RsaPss::verify()` return **true** for an encoding RFC 8017 rejects, where I had reasoned `hash_equals` would catch it. Also: a cycle stopped the whole walk instead of its branch, and an Ed25519 branch made redundant by its own repair. Infection could not be used; Pest's own mutation testing could | [`notes/step-65-mutation-testing.md`](notes/step-65-mutation-testing.md) |
| 66 | 2026-09-22 | The resource audit: a 256 MB asset costs 6 MB (the data hash really streams), reassembly is linear — but a manifest store of 63 MiB, **inside** the verifier's own 64 MiB bound, needs 132 MB and ends a 128 MB host with a PHP fatal error instead of an `Invalid`. A fatal is not failing closed. Measured beside it: the largest real store in 212 is 3.36 MB, the median 45 kB, and the PNG path costs twice what JPEG costs for the same bytes | [`notes/step-66-resource-audit.md`](notes/step-66-resource-audit.md) |
| 67a | 2026-09-22 | SPEC-024 tests first: five red, 369 passed. The hostile stores are generated in the temporary directory rather than committed — a block of `0x41` is not evidence, and the corpus already weighs 63 MB. Each verification runs in its own process, because `memory_limit` cannot be lowered from inside one; the harness learned at once that a process killed by the limit does not print *nothing* — PHP's fatal text goes to stdout, so "died" is "no line that parses as JSON" | [`notes/step-67-resource-bounds.md`](notes/step-67-resource-bounds.md) |
| 67b | 2026-09-22 | SPEC-024 implemented, 374 green: the bound falls to 16 MiB and a store that fits the bound but not the host is refused before it is read. The file that ended a 128 MB process in step 66 now costs 6.0 MB and 2 ms. The share was measured rather than assumed — peak is about twice the store plus six megabytes — and three older specs were amended, because SPEC-001 AC12, SPEC-002 AC13 and SPEC-003 AC15 each named the old figure | [`notes/step-67-resource-bounds.md`](notes/step-67-resource-bounds.md) |
| 68 | 2026-09-22 | The six amendments since step 58 on one page, replacing step 64's (never confirmed, three more since). Two are group A: the store bound falling from 64 MiB to 16 MiB with a host-relative refusal — the first amendment that declines to examine a file rather than judging it more strictly, and the first rule whose answer depends on the host — and the Ed25519 key kind read from the SPKI OID. 51 + 17 + 6 = 74, which is what the specs hold | [`notes/step-68-amendments-since-58.md`](notes/step-68-amendments-since-58.md) |
| 69 | 2026-09-22 | What a tag would freeze: **600 public symbols in 69 classes, none marked `@internal`**, against a README that documents two classes and four accessors. Drawing the contract at what a caller needs gives 99 symbols — sixteen per cent — except that `$report->store` re-admits the whole parse model through one property. Nothing changed; three proposals and a tag question for the maintainer | [`notes/step-69-api-surface.md`](notes/step-69-api-surface.md) |
| 70a | 2026-09-22 | SPEC-025 tests first: seven red, 374 passed. The contract is recorded as a file of ninety-one lines rather than an array in a test — a promise that changes should change visibly, as a diff somebody reads. AC3 and AC5 fail on what is missing from the code and the README rather than on missing tooling, which makes them tests of the decision instead of the checker | [`notes/step-70-public-api.md`](notes/step-70-public-api.md) |
| 70b | 2026-09-22 | SPEC-025 implemented, 381 green: nine classes are the contract, sixty are marked `@internal`, `$store` is an escape hatch that says so, and the README has a Public API section. The snapshot was made to catch something — a public method added by hand fails AC1 and `api-check` names it. AC3 was amended: silence about `$store` is the wrong rule, because a reader whose IDE offers it needs to be told, not left to infer | [`notes/step-70-public-api.md`](notes/step-70-public-api.md) |
| 71 | 2026-09-22 | The conformance suite run at last, and it is a *third* independent implementation, not a rubric — own extractor, parser and crypto, in Python. It calls `fixture-signed.jpg` `claimSignature.missing` where c2patool, this verifier and the Go one all accept it, while its own PNG and WebP paths validate the same claims: three readings against one, and the extraction is byte-identical, so the fault is theirs. The prize is its catalogue — **150 predicates over 237 normative C2PA 2.4 rules**, 101 of which apply here. Reading it found a stale message of ours: redactions are refused "before M7", and M7 closed in step 57 | [`notes/step-71-conformance-suite.md`](notes/step-71-conformance-suite.md) |
| 72 | 2026-09-22 | The redaction message told a user redactions were "not supported before M7" — and M7 closed in step 57, so the refusal read as temporary when it is deliberate. Restated with its real reason (§15.11.3.3.1 and `assertion.notRedacted`), and the test that pinned `M7` into the message restated with it. Also settled: step 71's worry that `ManifestGraph::$redactedAssertions` was unreachable was **wrong** — it is populated; the refusal adds a status without stopping the walk | [`notes/step-72-redaction.md`](notes/step-72-redaction.md) |
| 73 | 2026-09-22 | ISOBMFF measured before a spec is written: the store sits in **one** top-level `uuid` box with twenty-one bytes of preamble, and peeling those off lets the **existing stack read it unchanged** — so M8's first slice is a container extractor and nothing more. Two corrections to the brief: the assertion is `c2pa.hash.bmff.**v3**`, not v2, and Merkle is not on the first path. Where BMFF really differs: exclusions are box paths, not byte ranges | [`notes/step-73-isobmff-fixture.md`](notes/step-73-isobmff-fixture.md) |
| 74a | 2026-09-22 | SPEC-026 tests first: nine red, 381 passed, ten malformed variants built byte by byte. AC9 was a question and is now an answer — **AVIF is the same container in every respect** and comes along for free, measured with its own fixture and oracle rather than assumed. Running c2patool over the variants found two places where this spec is deliberately stricter: an unknown purpose (c2patool reports "no manifest") and `size == 0` on a box that is not the last (c2patool reads it anyway) | [`notes/step-74-isobmff-tests.md`](notes/step-74-isobmff-tests.md) |
| 74b | 2026-09-22 | SPEC-026 implemented, 390 green: an MP4 is read, and then refused by name — `Invalid` with `the hard binding c2pa.hash.bmff.v3 is not supported yet`, never a silent `Valid`. AC7 could not be true and was amended: `size == 0` is what *makes* a box the last one, so the case it asked to refuse cannot occur; what catches a box that swallows its successors is the hard binding, not the container. `StreamReader` is sequential and the first draft assumed it seeks. SPEC-024 amended to four containers — its test named three and could not see the fourth | [`notes/step-74-isobmff-tests.md`](notes/step-74-isobmff-tests.md) |
| 75 | 2026-09-22 | The three amendments since step 68, **all confirmed** — and the first round with no group A and no group B: not one changes a verdict. Two came out of writing code against an approved criterion and finding it wrong. The one worth a second look is SPEC-024 #1, which exists because a drift alarm that enumerates three constants could not see a fourth container arrive; PHPStan noticed instead. 51 + 17 + 6 + 3 = 77 | [`notes/step-75-amendments-since-68.md`](notes/step-75-amendments-since-68.md) |
| 76 | 2026-09-22 | The BMFF hash spec is **not** written, and this says why: c2pa-rs turns the box-path exclusions into flat byte ranges and then uses the same streaming hash as the data hash, adding the excluded range's start offset as a big-endian `u64` into the digest — but six attempts to reproduce the stored hash of our own fixture all failed. A spec whose central criterion cannot be measured is one this project does not write. What the next attempt should do: instrument c2pa-rs rather than read more of it | [`notes/step-76-bmff-hash-unfinished.md`](notes/step-76-bmff-hash-unfinished.md) |
| 77 | 2026-09-22 | The BMFF hash **reproduced**, in one run, by instrumenting c2pa-rs instead of reading it: for each top-level box no exclusion matches, hash its own offset as a big-endian `u64` and then its bytes. Verified byte for byte on both the MP4 and the AVIF. Step 76 spent an afternoon on six wrong hypotheses; two `eprintln!` lines settled it — the third time in this project that a measurement was cheaper than the next reading | [`notes/step-77-bmff-hash-reproduced.md`](notes/step-77-bmff-hash-reproduced.md) |
| 78a | 2026-09-22 | SPEC-027 tests first: seven red, 390 passed. The blocking question is **answered by measurement, not assumed** — and it had a sharp edge: `ftyp` must be the first box in a valid ISOBMFF file and is always excluded, so no c2patool output can ever have an included first box. Making the exclusion miss (`/ftyp` → `zzzz`, four bytes, every CBOR length unchanged) produced one, and the instrumented c2pa-rs printed `marker offset=0`: a marker before **every** included box. `box-moved` is the variant that matters — identical hashed bytes, different offsets, mismatch | [`notes/step-78-bmff-hash-tests.md`](notes/step-78-bmff-hash-tests.md) |
| 78b | 2026-09-22 | SPEC-027 implemented, 397 green: **an MP4 and an AVIF verify whole**, hard binding included, and a video whose pixels were replaced after signing is now `Invalid` because somebody checked. Three drift alarms rang and all three were right — the API snapshot (91 → 93 symbols, two lines of diff), the enum count, and two exhaustive success lists. Amendment 1: `checks_performed` said `dataHash` for a check that never ran, and now says `bmffHash` (weight B) | [`notes/step-78-bmff-hash-tests.md`](notes/step-78-bmff-hash-tests.md) |
| 79 | 2026-09-22 | The one amendment since step 75, **confirmed**: `checks_performed` says `bmffHash` where the BMFF hash ran, instead of naming a data-hash check that never happened. Weight B — the report's shape, no verdict. 78 amendments confirmed, none outstanding | [`notes/step-79-amendment-since-75.md`](notes/step-79-amendment-since-75.md) |
| 80 | 2026-09-22 | **M8 is not done, and three tracked files said it was.** SPEC-027 closing was treated as the milestone closing; they are different, and M8's own description names Merkle trees. Worse: MOV was claimed in the README, the comparison table and the milestones before it was measured — it does verify, but that is luck, not method, and a claim that happens to be true reads exactly like one that was checked. Corrected here; nothing in `src/` changed | [`notes/step-80-m8-is-not-done.md`](notes/step-80-m8-is-not-done.md) |
| 80b | 2026-09-22 | MOV and HEIC made into fixtures with their oracles, so the claim is held by tests rather than by my word — all four ISOBMFF flavours `Trusted` here and `Valid` at c2patool, 397 green. HEIC had to be *made* (macOS `sips`, from this repository's own PNG) because nothing reachable had one. Two amendments, and the rule that was missing: no flavour may be named in the README, the comparison table or a milestone row unless a fixture here holds it | [`notes/step-80-m8-is-not-done.md`](notes/step-80-m8-is-not-done.md) |
| 81 | 2026-09-22 | The two amendments since step 79, **confirmed**, both group C. The one worth an eye is SPEC-026 #2: the first amendment here written because of something *said* rather than something built — AC9 asked for the flavour in hand and had nothing to say when the README grew two more. Its new rule is about the record, not the code, because what went wrong was not a bug. 80 confirmed, none outstanding | [`notes/step-81-amendments-since-79.md`](notes/step-81-amendments-since-79.md) |
| 82 | 2026-09-22 | Fragmented BMFF measured whole: the init carries the manifest with a `merkle` list and **no `hash`**, each fragment carries its own C2PA box with `purpose: merkle` holding its leaf index and proof. All three rules reproduced — `initHash` and each leaf are the *same* v3 digest already built, and the tree puts the largest power of two on the left. Seven fragments were needed to pin that: the obvious bit-rule verifies four of five leaves and fails the fifth, which is the most dangerous kind of wrong | [`notes/step-82-fragmented-bmff.md`](notes/step-82-fragmented-bmff.md) |
| 83a | 2026-09-22 | SPEC-028 tests first: six red, 398 passed. AC4's oracle measured before the criterion was asserted — and two things about *how* c2patool answers matter: it replies in **text, not JSON**, and it gives the same code for a changed init, a changed fragment and a foreign one, **never saying which file failed**. Our AC2–AC4 each require the file to be named, so this verifier will be more specific than its oracle. AC5 needs no fixture at all: a withheld or repeated fragment is a different *set*, not a broken file | [`notes/step-83-fragmented-tests.md`](notes/step-83-fragmented-tests.md) |
| 83b | 2026-09-22 | SPEC-028 implemented, 404 green — **a fragmented DASH stream verifies**, init segment and every fragment, and **M8 is closed**. It stayed small because `Verifier` already takes its `BmffHashCheck` as an argument: `FragmentedVerifier` hands it one that knows this call's fragments, so `Verifier` learns nothing about DASH and no status is patched afterwards. PHPStan caught an injected `Verifier` that was never read — a seam that was not one — and removing it shrank the contract from 96 symbols to 95. Both contract alarms rang on their own | [`notes/step-83-fragmented-tests.md`](notes/step-83-fragmented-tests.md) |
| 84 | 2026-09-22 | The one amendment since step 81, **confirmed**: the contract grows to ten classes with `FragmentedVerifier` — the first class ever *added* to the promise rather than merely recorded. Weight B; nothing that existed changed. Both contract alarms rang without anybody having to remember them, which is exactly what step 75 asked of an alarm. 81 confirmed, none outstanding | [`notes/step-84-amendment-since-81.md`](notes/step-84-amendment-since-81.md) |
| 85 | 2026-09-22 | Every public fixture re-checked against its source. `c2pa-org/public-testfiles`: all 25 held, **byte-identical**, and the 2.2 tree is still empty. `c2pa-rs`: nine ISOBMFF files we never had, because step 39 predated M8 — eight agree with c2patool, and one does not. **`video1.mp4` carries `c2pa.hash.bmff.v2`**, which SPEC-027 put out of scope "if a file with one ever turns up". One has, in the reference implementation's own fixtures, and it also holds the only trusted timestamp of any ISOBMFF file here | [`notes/step-85-public-fixtures.md`](notes/step-85-public-fixtures.md) |
| 86 | 2026-09-22 | `c2pa.hash.bmff.v2` measured: **it is not another digest, it is another exclusion list** — and v2's uses exactly the three filters SPEC-027 named out of scope. Six of its eight paths are nested, four carry a `subset`, two carry `flags`. Instrumented c2pa-rs confirms the digest is identical: markers only before included top-level boxes, nested exclusions punching holes without new markers, and `subset` length 0 meaning to the end of the box (two `stco` boxes, 40 bytes, excluded from offset 16). Implementing v2 is one function growing and one label | [`notes/step-86-bmff-v2.md`](notes/step-86-bmff-v2.md) |
| 87a | 2026-09-22 | SPEC-029 tests first: six red on `boxTree()` and `plan()`, AC7 green from birth. AC3's expected value is a **transcript** of what the instrumented c2pa-rs printed — six ranges with their markers — so a misplaced box shows up as a number that moved. AC6 pulls against itself on purpose: `flags` must be refused while `video1.mp4`'s own two `flags` exclusions on `/moof` paths must not make it fail, so the refusal has to come from resolving a path that exists. And the variadic `toContain` trap for the **eleventh** time, this time disguising a green test as a red one | [`notes/step-87-bmff-v2-tests.md`](notes/step-87-bmff-v2-tests.md) |
| 87b | 2026-09-22 | SPEC-029 implemented, **411 green**: `c2pa.hash.bmff.v2` verifies, nested exclusion paths and `subset` included, and `c2pa-rs`'s own `video1.mp4` stops being refused. Three criteria had to move first, each with an amendment before a test was allowed to follow. The sharpest: **AC1 was comparing two verifiers that did not have the same anchors** — `c2patool` falls back to the operating system's trust store for a timestamp authority and therefore judges this file's 2022 signers at the moment they were stamped; this verifier has no system store by design and judged them at *now*, correctly adding `signingCredential.expired`. Asked under the same anchors, the two agree status for status, in both scopes. A drift nothing was watching also surfaced: `bin/api-check.php` had nine contract classes where the test that guards it has ten | [`notes/step-87-bmff-v2-tests.md`](notes/step-87-bmff-v2-tests.md) |
| 88 | 2026-09-22 | The three amendments since step 84, **confirmed**: 84 in total, none outstanding. All three come from 87b and none changes a rule. The one worth reading is SPEC-029 #1 — a criterion that compared **two verifiers with different anchors**. `c2patool` falls back to the operating system's trust store for a timestamp authority and so judges a 2022 signer at the moment it was stamped; this verifier has no system store by design, judged it at *now*, and correctly called it expired. The temptation was to relax the criterion to whatever we produce; what it needed was to ask both sides the same question, with an anchor fixture that has existed since M6 | [`notes/step-88-amendments-since-84.md`](notes/step-88-amendments-since-84.md) |
| 89 | 2026-09-22 | `bin/api-check.php` is a step of `composer check`, so it runs in CI on all three PHP versions — and, more to the point, **the contract list stopped existing twice**. The script held nine classes and the test ten, and they drifted for four commits because the script was something a maintainer ran by hand. `apiContract()` and `apiClass()` now live in the script and the test reads them. Falsified rather than assumed: a deliberate extra line in the recorded surface makes `composer api-check` exit 1 | [`notes/step-89-api-check-in-green.md`](notes/step-89-api-check-in-green.md) |
| 90 | 2026-09-22 | **All 111 applicable conformance predicates laid next to this verifier**, one by one, in [`docs/conformance.md`](docs/conformance.md) — 49 enforced, 12 partial, 7 closed, 21 out of scope by design, **22 gaps**. First correction: it is 111, not step 71's 101; closing M8 added the ISOBMFF and streaming families. The finding that matters: **OCSP responses stapled into the manifest are not read**, and that needs no network. Measured on `ocsp.jpg` — `rVals.ocspVals[0]` really holds 2264 bytes of DER, `Cert Status: good`, and `CoseSign1` already parses the header into a field nothing reads. Eight more gaps are laxer-than-2.4 assertion rules that cannot let a changed byte through; the rest are stricter or differently named | [`notes/step-90-conformance-mapping.md`](notes/step-90-conformance-mapping.md) |
| 91 | 2026-09-22 | **SPEC-030 approved**: stapled OCSP. The spec's whole shape comes from one measurement — `rVals` sits in the **unprotected** COSE bucket, so anyone holding the file can add, alter or strip it. Four consequences follow: a stapled response may never raise trust, an unverifiable one may never fail the file (editing one unsigned byte would otherwise be a denial vector against any valid asset), only a cryptographically verified `revoked` may lower trust, and absence proves nothing — which is why `signingCredential.ocsp.skipped` on every other file matters. Ten criteria. All four questions answered on approval: a stale `good` expires and a stale `revoked` does not; AC3's fixture is constructed with throw-away keys, making it **the first criterion here with no second implementation behind it**; `notRevoked` is recorded with its caveat in the explanation itself | [`specs/SPEC-030-stapled-ocsp.md`](specs/SPEC-030-stapled-ocsp.md) |
| 92a | 2026-09-22 | SPEC-030 tests first: **nine red**, AC9 green as the alarm. An amendment before a line was written — AC1 asked for `ocsp.jpg` *without* trust settings, and measured that gives `Invalid` (the step-87b divergence, on a second file: c2patool falls back to the system trust store for the TSA) *and* a stale response, which is the opposite of what AC1 wanted. With the anchor the judged time is the attested 2025-08-13 and the response is fresh, so **the same file is AC1 and AC6** — which one a run gets depends on the anchor, not on the bytes. `bin/make-ocsp-variants.php` builds the four DER responses nobody can download, shreds every key before it ends, and refuses to finish if one is left. AC3 gives up end-to-end coverage (no CBOR writer here) and is **the first criterion in this project with no second implementation behind it** | [`notes/step-92-ocsp-tests.md`](notes/step-92-ocsp-tests.md) |
| 92b | 2026-09-22 | **SPEC-030 implemented, 421 green**: this verifier reads the OCSP responses a signer staples into its own signature, so a certificate its own manifest reports as revoked is no longer called trusted — and **every file now says whether revocation was checked at all**. The spec's own OID was wrong (`id-pkix-ocsp` where `id-pkix-ocsp-basic` belonged), and the fixture caught it: following the spec as written would have skipped every response and left the feature silently inert while ten criteria passed. Two tests were green *because* of that bug and went red when it was fixed. Twenty-four alarms fired on the report growing by one line per file; each was updated to record the growth. Five conformance gaps closed, 17 left, none able to produce a wrong `Trusted` | [`notes/step-92-ocsp-tests.md`](notes/step-92-ocsp-tests.md) |
| 93 | 2026-09-22 | The three amendments since step 88, **confirmed**: 87 in total, none outstanding. Two of them are the record of a mistake that would otherwise have been invisible — SPEC-030's Scope gave the wrong OID for `id-pkix-ocsp-basic`, and had the code followed it, **all ten criteria would still have passed while the feature did nothing at all**. What caught it was a fixture that could not be downloaded and had to be built, from an independent source of the same number. AC7 and AC8 recorded it from the other side: green while the bug was there, red when it was fixed | [`notes/step-93-amendments-since-88.md`](notes/step-93-amendments-since-88.md) |
| 94 | 2026-09-23 | **A first-version notice at the top of the README**, on the maintainer's decision: public maybe, but not yet, and when it happens it must be unmistakable that nobody has used this. The sentence it turns on: *"This code is thoroughly tested and has never been used. Those are two different things and both are true."* Neither half may swallow the other — hiding 421 tests and three compared implementations behind modesty is its own dishonesty, and so is letting them stand in for use. A new section asks for the one measurement this project cannot make for itself: run `c2patool` on the same file and report where the two differ, **from the cheap shared hosting this library exists for**. Advice corrected: a first tag should be `0.1.0`, not step 88's `0.2.0` | [`notes/step-94-first-version-notice.md`](notes/step-94-first-version-notice.md) |
| 95 | 2026-09-23 | **The disclosure now says what "controlled" means.** It already said the project is written with Claude Code and reviewed; what it did not say is what the review consisted of. The section now lists the controls a reader can check — a spec before code with a build-enforced traceability table, tests seen failing first, measured kept apart from reasoned in every entry, 87 amendments confirmed rather than quiet edits, three independent oracles, and the 17 unmet obligations published too — with every number verified against the repository before it went in. Plus the line that keeps it honest: **none of that is an independent security audit**, which is the step-94 notice from the other direction | [`notes/step-95-disclosure.md`](notes/step-95-disclosure.md) |
| 96 | 2026-09-23 | **Read as a stranger would read it, before the switch.** The four irreversible checks are clean: no private key in any of 276 commits, no assistant attribution in any commit message, no local paths, and the only mentions of the untracked instruction file are `.gitignore` and the records of this same audit. The *texts* were not clean: **`SECURITY.md` still said ISOBMFF hashes and OCSP were not checked**, which M8 and SPEC-030 made untrue; the changelog stopped at M7, missing eight specs and a milestone; the Packagist description named neither; and the drift-alarm corpus was "96 files from nine writers" where it is 93 and the writers are uncountable. All corrected, every surviving number counted against the repository, and the README's no-network / no-exec / no-temp-file claims verified mechanically. **Nothing was made public** | [`notes/step-96-pre-public-audit.md`](notes/step-96-pre-public-audit.md) |
| 97 | 2026-09-23 | **One address rewritten out of 277 commits, and everything else kept.** The alternative on the table was a scrubbed copy with less history; that was declined, because the history is the evidence for the README's *judge the method, not the tool* — and the audit had already shown there was nothing else to hide. `git filter-repo` with a mailmap, run **in a throwaway clone**, verified before anything was pushed: 277 commits, the tree of `HEAD` byte-identical, all 277 compared line for line on tree, dates, name and subject, 554 of 554 e-mail fields rewritten, 0 traces of the old one. The consequence nobody asks about: **199 commit references in `AI-LOG.md` and twelve other files became dead pointers**, and all were translated through `filter-repo`'s commit-map — an edit to a faithful record, which the note says plainly, changing a pointer and not a fact | [`notes/step-97-history-rewrite.md`](notes/step-97-history-rewrite.md) |
| 98 | 2026-09-23 | **De repository is publiek.** Voorafgegaan door de audit van stap 96 (vier onomkeerbare controles, schoon) en de herschrijving van stap 97 (het auteursadres uit alle 277 commits, inhoud byte-identiek). Eindcontrole vlak ervoor: werkboom schoon, niets ongepusht, 0 treffers van het oude adres, CI groen. Wat een bezoeker als eerste leest is de waarschuwing uit stap 94 — *thoroughly tested and never used* — met daaronder wat er bekend ontbreekt | — |
| 99 | 2026-09-23 | **`main` beschermd**: force-push en verwijderen geblokkeerd, ook voor beheerders. Eén ding uit het voorstel is bij nader inzien *niet* aangezet — verplichte statuschecks blokkeren ook **directe** pushes, niet alleen merges, en dit project pusht rechtstreeks naar `main`; aanzetten zou elke push weigeren tot CI op die commit gedraaid heeft. Getest met een push, die er doorkwam; die test staat als lege commit `fe3e6d2` in de geschiedenis, en is onverwijderbaar geworden door precies de bescherming die hij testte | — |
| 100 | 2026-09-23 | **`v0.1.0` voorbereid**: het `Unreleased`-blok van het changelog is de release geworden, en de README vertelt hoe een tester hem vandaag al installeert — via een `vcs`-repository, zolang Packagist nog niet bestaat. `0.1.0` en niet `0.0.1`, om een mechanische reden: Composer leest `^0.0.1` als *precies* 0.0.1, dus elke latere fix zou niemand bereiken en iedereen dwingen zijn `composer.json` aan te passen. `^0.1` krijgt elke 0.1.x-fix vanzelf — minder taggen, niet meer | — |
| 101 | 2026-09-23 | **`v0.1.0` gepusht** — en meteen nagerekend wat een gebruiker binnenhaalt: het archief van de tag bij GitHub is exact wat `bin/package-check.php` lokaal meet, 245 bestanden. Daarbij viel een fout van mijzelf op: de changelog-regel over SPEC-023 zei dat `notes` en `specs` buiten het pakket blijven, en dat is omgekeerd. Ze gaan er bewust in, omdat SPEC-023 eist dat elke relatieve link in meegeleverde markdown wijst naar iets dat óók meegeleverd wordt — en de README en het log verwijzen ernaar. De zin die dat verkeerd vertelde schreef ik in stap 96, de stap die de buitenteksten op juistheid moest controleren | — |
| 102 | 2026-09-23 | **Op Packagist**, en de README zegt het nu ook: het `vcs`-blokje is vervangen door één regel `composer require provemark/c2pa-verifier`. De webhook die Packagist bij elke push laat bijwerken is gezet (dezelfde vorm als bij `content-credentials`, laatste aflevering OK) — deze push is er meteen de proef op | — |
| 103 | 2026-09-23 | **De 17 bekende gaten zijn twaalf issues geworden**, met `Discussions` aan. Elk issue noemt het predicaat, wat de verifier vandaag doet, **wat het ontbreken kost** — laxer dan 2.4, strenger dan 2.4, of alleen rapportage — waar het zou wonen, en of het het publieke contract laat groeien. Daarmee wordt `docs/conformance.md` van een disclaimer een uitnodiging. Elk issue sluit af met de regel die een teleurstelling voorkomt: dit project neemt geen code-eerst-PR aan, begin in het issue met wat de regel zou moeten zijn | — |
| 104 | 2026-09-23 | **Een rode CI die ik eerder had moeten zien.** SPEC-030 zette een tweede *nu* in een uitleg — het beoordeelde moment bij `signingCredential.ocsp.skipped` — en de CLI-test vergelijkt de uitvoer van twee aanroepen die een secondegrens kunnen straddelen. Er stond al een normalisator voor precies dat, maar die dekte alleen `expired at …`. Twee runs vielen erover, op PHP 8.4 terwijl 8.3 en 8.5 slaagden, wat een race eruit laat zien. **Eén ervan was de commit waar `v0.1.0` op staat**, en die had ik niet nagekeken: na elke push die dag wel, na de release-push niet | — |
| 105 | 2026-09-23 | **`pest --parallel` werkt** — was 24 fouten, nu 421 groen in **2,6 s** tegen 7,4 s serieel. De oorzaak was geen wankele test maar een koppeling die de seriële runner verbergt: van 190 functies in de suite werden er **acht** uit een ander bestand gebruikt, plus **één constante** van de 45. Die negen staan nu in `tests/Shared.php`, die de bootstrap voor elke worker laadt. Twee missers onderweg vastgelegd: de eerste analyse zocht alleen functies (de constante bleef over, vier fouten), en het verplaatsen sneed een bestandskop mee. `composer check` blijft serieel; `composer test:parallel` is de snelle lus | [`notes/step-105-parallel-tests.md`](notes/step-105-parallel-tests.md) |
| 106 | 2026-09-23 | **De parallelle run staat in CI**, als stap na `composer check` op alle drie de PHP-versies. Niet om snelheid: serieel laadt Pest alles in één proces, dus een helper uit het ene bestand die in het andere wordt gebruikt werkt gewoon en de koppeling blijft onzichtbaar. Zo stapelden er negen op terwijl de suite bij elke push groen stond. Drie seconden per versie houdt die eigenschap vast | — |
| 106b | 2026-09-23 | **Een regel voor de fout die dit project dertien keer maakte.** `toContain()` is variadisch, dus `toContain($needle, $message)` controleert of de hooiberg óók die melding bevat — en de melding is weg. De regel staat bij de andere controles op de werkwijze (SPEC-000) en leest de code met **PHP's tokenizer**: de eerste versie zocht in tekst en meldde vijf overtredingen, waarvan er drie de waarschuwende commentaren waren die dit juist uitleggen. Een regel die zijn eigen documentatie afkeurt is erger dan geen regel. Hij vond één echte, live: `not->toContain(GeneralError, $fixture)` | [`notes/step-106-tocontain-rule.md`](notes/step-106-tocontain-rule.md) |
| 107 | 2026-09-24 | **`c2patool` 0.28.0 measured against the shared settings file**, because `c2pa` 0.91.0 deprecates `trust.trust_anchors` for a `trust.anchors` list, with removal announced for 0.92.0 (mid-November 2026). 32 runs per tool (two fixtures × no settings and 15 settings files). Anchors still work, unchanged. **`trust.allowed_list` is silently ignored**: `allowed-only` and `allowed-plus-wrong-root` go from `Trusted` to `Valid` + `signingCredential.untrusted`, while this verifier still says `Trusted`. The same file now means two things depending on the oracle's version. `signingCredential.ocsp.skipped` is new in 0.28.0 and already emitted here since SPEC-030 | [`notes/step-107-c2patool-0.28.md`](notes/step-107-c2patool-0.28.md) |
| 107+ | 2026-09-24 | **SPEC-031 drafted**: `trust.anchors` read next to the legacy string, and a loose `trust.allowed_list` **refused** rather than silently dropped, the maintainer's call. Probed first: 13 settings shapes against `c2patool` 0.28.0. `trust_kind` separates nothing there, which is why the draft's first open question is a blocker. The corpus run found the larger issue: **14 of 281 files change under 0.28.0**, six from `Valid` to `Invalid`, five of them on a new data-hash exclusion rule. Re-pinning the oracle is its own step | [`specs/SPEC-031-trust-anchors-list.md`](specs/SPEC-031-trust-anchors-list.md) |
| 108 | 2026-09-24 | **A third wrong `Trusted`, found through the new oracle.** Three official Truepic test files exclude the EXIF segment *together with* the store in one range; C2PA 2.4 (VAL-ASSE-0043/0044) says the store's exclusion may hold only the store and padding, or else `assertion.dataHash.mismatch`. A copy with its EXIF capture date changed from 2023 to 2019 stays **`Trusted`** here and in `c2patool` 0.27.22. `c2pa-rs` 0.91.0 closed it with an exact-equality rule. The hole is SPEC-012 amendment 5 (*cover, not equal*), which was checked against the oracle and not against the specification. `docs/conformance.md` has called `PRED-IMG-004` enforced since step 90, and it is not. An equal rule changes the verdict of exactly these three of 165 files | [`notes/step-108-exclusion-wider-than-store.md`](notes/step-108-exclusion-wider-than-store.md) |
| 109 | 2026-09-24 | **Closed: an exclusion wider than the store.** SPEC-012 amendment 7 reverses amendment 5. Every store piece must still lie in an exclusion, and now **an exclusion holding any part of the store holds nothing else**, else `assertion.dataHash.mismatch` naming the extra bytes, and the file is not hashed. Seen red on the Truepic file first. The one other criterion it moved, SPEC-017 AC6, keeps its point (the timestamp trusted, nothing expired) and gains the mismatch. 864 runs before and after: only the Truepic lines differ, and only under their root does a verdict change, `Trusted` → `Invalid`. The EXIF-dated copy is `Invalid`. `PRED-IMG-004` is enforced now, and the table says since when | [`notes/step-109-exclusion-holds-only-the-store.md`](notes/step-109-exclusion-holds-only-the-store.md) |
| 110 | 2026-09-24 | **The 2.4 text settled what the draft had guessed.** §14.4.2: TSA anchors *shall* be separate from signers', which confirms SPEC-031's answer. §14.4.3: the allowed list *shall not* apply to time-stamps, so the draft's TSA allowed list is gone, and today's `TimestampCheck` turns out to pass it along anyway (read, not yet measured). A throwaway signer with an EKU no list knows answered the per-entry `trust_config`: **an entry's EKUs never widen another's**. The draft's union would have said `Trusted` where `c2patool` 0.28.0 and §14.4.1 say `Invalid`. Found on the way: a one-certificate `x5chain`, which RFC 9360 allows, is refused here | [`notes/step-110-trust-lists-in-2.4.md`](notes/step-110-trust-lists-in-2.4.md) |
| 111a | 2026-09-24 | **SPEC-031 approved, and its tests seen red: eight of eight.** One script writes the new-shape settings, a probe signed under an EKU no list knows (keys shredded, exit non-zero if one survives) and `c2patool` 0.28.0's 25 answers. Six tests are red on *"unknown key trust.anchors"*, one because a loose `allowed_list` is still accepted, one on messages that do not name the entry. **One false green was caught first**: AC5's needle `trust.anchors` was in today's refusal message for the wrong reason | [`notes/step-111-trust-anchors-tests.md`](notes/step-111-trust-anchors-tests.md) |
| 111b | 2026-09-24 | **SPEC-031 implemented, 430 green**: `trust.anchors` is read, and every entry counts only for its own kind. An entry's EKUs widen only the chains that reach it, and a loose `allowed_list` is refused with directions. The drift test (AC7) caught one sentence too many: an untrusted outcome named every entry of another kind, including one that changed nothing. It now names only an entry the chain *would have reached*. The API check moved four helpers off the contract before they became promises. **The next tag should be `0.2.0`**: a settings file that worked in 0.1.0 is refused now | [`notes/step-111-trust-anchors-tests.md`](notes/step-111-trust-anchors-tests.md) |
| 112 | 2026-09-24 | **`eku-c2pa.png` is not a wrong `Valid`: the oracle moved.** Three throwaway signers showed that `c2patool` 0.28.0 no longer accepts the C2PA claim-signing EKU (or documentSigning) by default, even though C2PA 2.4 §14.4.1 makes claim-signing *the* signer EKU and 0.27.22 accepted it. Nobody noticed because every real signer measured also carries emailProtection. This verifier keeps SPEC-015's list and names the divergence. Found on the way: §14.5.1 says anchors belong to EKUs, which neither this verifier nor `c2patool` models; that is its own step | [`notes/step-112-claim-signing-eku.md`](notes/step-112-claim-signing-eku.md) |
| 113 | 2026-09-24 | **§14.5.1 says anchors belong to EKUs, and nothing enforces it.** Under the official C2PA list, two corpus files reach an anchor (Pixel 10, OpenAI), and both carry the claim-signing EKU on leaf and issuing CA, as the Certificate Policy requires. Neither this verifier nor `c2patool` ties an anchor to an EKU, and the shared settings format has no way to say which anchor is for which EKU. The conformance catalogue has no predicate for it. A possible wrong `Trusted` that needs a CA to misissue first; no file shows it | [`notes/step-113-anchors-and-ekus.md`](notes/step-113-anchors-and-ekus.md) |
| 114 | 2026-09-24 | **The allowed list still reaches a time-stamp, but only through PHP.** A TSA certificate put on `TrustSettings`' allowed list with the constructor makes the time-stamp trusted, and the signer is judged at the token's time. On the Truepic file that makes an **expired signer stop being expired**. §14.4.3 says the allowed list *shall not* apply to time-stamps. `fromJson()` cannot reach it since SPEC-031. `c2pa-rs` does the same, read from its code, because c2patool trusts these TSAs without any anchor and cannot be asked. The fix is one argument in `tsaSettings()` | [`notes/step-114-allowed-list-and-time-stamps.md`](notes/step-114-allowed-list-and-time-stamps.md) |
| 115 | 2026-09-24 | **The allowed list no longer reaches a time-stamp.** One argument in `TimestampCheck::tsaSettings()`, a new criterion SPEC-017 AC13 that was red on that seam first, 431 green, and no other test moved. This verifier is now stricter than `c2pa-rs` on a *shall not* of §14.4.3. Found while numbering: a test called *"SPEC-017 AC12"* from step 46 has never had a criterion or a traceability row, and the spec check did not see it | [`notes/step-114-allowed-list-and-time-stamps.md`](notes/step-114-allowed-list-and-time-stamps.md) |
| 116 | 2026-09-24 | **The spec check now reads the other direction too**: a test that claims a criterion has to find a Traceability row for it. Measured first: 394 declarations, 376 claiming a criterion, and **five orphans**. Four were described in an amendment's text and never given a row (the matrix AC16–18, the Ed25519 AC11). One, SPEC-017 AC12 for the Pixel 10 file, was never written down at all. The check's first run on this repository named all five, and also its own new AC11, before that row existed | [`notes/step-116-named-criteria.md`](notes/step-116-named-criteria.md) |
| 117 | 2026-09-24 | **A chain of one, as RFC 9360 writes it.** A signer directly under a root gets its `x5chain` as a bare byte string, which the RFC prescribes and both `c2patool` versions accept. This verifier had refused it since step 16, because no real signer lacks an intermediate. A signed fixture now shows it: `Valid` without settings, `Trusted` with the root, in both oracles and here. The byte string gets the same rules as an array element. Step 116's rule rang on its first real use: AC13 stopped the build until its row existed | [`notes/step-117-one-certificate-chain.md`](notes/step-117-one-certificate-chain.md) |
| 118 | 2026-09-24 | **A hard binding the claim only gathered is not its own.** The last pass over the `c2patool` 0.28.0 differences found one file still `Valid` here that 0.28.0 refuses: step 48's variant with `c2pa.hash.data` in `gathered_assertions`. C2PA 2.4 §10.2.2 says `created_assertions` *shall* reference the hard binding. Step 48 had followed `c2pa-rs` 0.90, which did not look. Not a byte hole, because the gathered binding was verified and matched, but a manifest the specification calls malformed. Now `claim.hardBindings.missing`. Over 870 runs only that file changes | [`notes/step-118-hard-binding-in-gathered.md`](notes/step-118-hard-binding-in-gathered.md) |
| 119 | 2026-09-24 | **The oracle texts caught up.** `docs/comparison.md` now says what 0.28.0 changed and that every one of its 14 changed files has been examined. It also lost two sentences that had stopped being true: that no second implementation had been run (Go ran in step 61), and that no production trust list had been used (step 113). The README counts **87 confirmed amendments and eleven written today that await confirmation**, rather than folding them into one number | — |
| 120 | 2026-09-24 | **The eleven amendments since step 93, on one page, for confirmation.** Six change a rule, and all six have the same origin: the first measurement against `c2patool` 0.28.0. The one worth reading twice is SPEC-012 #7, the third wrong `Valid` this project found in itself and the first that shipped. It came from a decision, not a slip: a relaxation checked against the tool and not against the text | [`notes/step-120-amendments-since-93.md`](notes/step-120-amendments-since-93.md) |
| 120b | 2026-09-24 | **All eleven confirmed** by the maintainer. Each amendment in its spec carries the stamp, the page's table says so row by row, and the README's count is 98, every one confirmed, none outstanding | [`notes/step-120-amendments-since-93.md`](notes/step-120-amendments-since-93.md) |
| 121 | 2026-09-24 | **Fuzzed again, 0 faults**: 7,722 mutated files, and every one of the 204 that stayed `Valid` is `Valid` in both `c2patool` versions too; 10,500 mutated settings files, and nothing but `TrustException` escaped the new parser. **Four known gaps put to both oracles.** A `c2pa.created` without `digitalSourceType` is `Invalid` in 0.27.22 *and* 0.28.0 and `Trusted` here, a leniency named in SPEC-018 that no corpus file had shown until this probe. A forbidden external-reference label is `Invalid` in 0.28.0, per 2.4 §15.10.3.2.2. No one enforces issue #1 or issue #2's general case | [`notes/step-121-fuzz-and-four-gaps.md`](notes/step-121-fuzz-and-four-gaps.md) |
| 121+ | 2026-09-24 | **SPEC-032 drafted**: the two rules from step 121 that the oracles enforce. A `c2pa.created` without `digitalSourceType`, in v2 claims only: a corpus scan found 19 such actions, 18 of them in v1 claims, which `c2pa-rs` does not check either, so no verdict should move. And the external-reference checks of 2.4 §15.10.3.2.2, which bring one new status code. Open question 2 is a blocker: the first rule is `c2patool`'s, and §15 does not state it | [`specs/SPEC-032-created-source-type-and-external-references.md`](specs/SPEC-032-created-source-type-and-external-references.md) |
